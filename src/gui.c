#include "gui.h"

button_t *CreateButton(int main_color, int border_color,
                       int x, int y, int width, int heigth,
                       char *text,
                       void (*function) (int, ...))
{
    // malloc new space 
    // populate new struct
    // get width of text
    // calculate corners
    // return button
}